<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cnu.zyx.backend.mapper.AiChatMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat(user_id, question, answer)
        VALUES(#{userId},#{question},#{answer})
    </insert>

    <select id="selectByUserId" resultType="com.cnu.zyx.backend.entity.AiChat">
        SELECT * FROM ai_chat WHERE user_id = #{userId} ORDER BY create_time ASC
    </select>

</mapper>